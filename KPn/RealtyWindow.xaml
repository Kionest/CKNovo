<Window x:Class="KPn.RealtyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KPn"
        mc:Ignorable="d"
        Title="Объект недвижимости" Height="520" Width="460"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource PrimaryLightBrush}">

    <Border Margin="15" Padding="20" Background="White" CornerRadius="10" BorderBrush="#A0C7F5" BorderThickness="1">

        <StackPanel>

            <TextBlock Text="Объект недвижимости" Style="{StaticResource SectionTitle}"/>

            <TextBlock Text="Адрес"/>
            <TextBox x:Name="AddressBox"/>

            <TextBlock Text="Площадь (м²)"/>
            <TextBox x:Name="SquareBox"/>

            <TextBlock Text="Этаж"/>
            <TextBox x:Name="FloorBox"/>

            <TextBlock Text="Владелец"/>
            <ComboBox x:Name="OwnerBox" DisplayMemberPath="FullName"  SelectedValuePath="OwnerID"/>

            <TextBlock Text="Тип объекта"/>
            <ComboBox x:Name="TypeBox" DisplayMemberPath="TypeName" SelectedValuePath="RealtyTypeID"/>

            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <CheckBox x:Name="IsOwnedBox" Content="В собственности"/>
                <CheckBox x:Name="IsActiveBox" Content="Активен"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"  Margin="0,20,0,0">
                <Button Content="Сохранить" Width="110" Click="SaveButton_Click"/>
                <Button Content="Отмена" Width="110" Margin="10,0,0,0" Click="CancelButton_Click"/>
            </StackPanel>

        </StackPanel>
    </Border>

</Window>
