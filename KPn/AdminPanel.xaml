<Window x:Class="KPn.AdminPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KPn"
        mc:Ignorable="d"
        Title="Панель администратора" 
        Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Управление пользователями" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" x:Name="dgUsers" AutoGenerateColumns="False" 
                  CanUserAddRows="False" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding UserID}" Width="50"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}" Width="150"/>
                <DataGridTextColumn Header="Роль" Binding="{Binding Role.Name}" Width="120"/>
                <DataGridTemplateColumn Header="Действия" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Удалить" Click="BtnDeleteUser_Click" Background="Red" Foreground="White" Padding="5,2" Margin="2" FontSize="10"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="2" Header="Добавить нового пользователя" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:" VerticalAlignment="Center" Margin="0,0,5,5"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtNewLogin" Margin="0,0,10,5" Height="25"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="Пароль:" VerticalAlignment="Center" Margin="0,0,5,5"/>
                <PasswordBox Grid.Row="0" Grid.Column="3" x:Name="txtNewPassword" Margin="0,0,10,5" Height="25"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Роль:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox Grid.Row="1" Grid.Column="1" x:Name="cmbRoles" DisplayMemberPath="Name" Margin="0,0,10,0" Height="25"/>

                <Button Grid.Row="1" Grid.Column="4" Content="Добавить пользователя" Click="BtnAddUser_Click" Background="Green" Foreground="White" Padding="10,5" Height="30"/>
            </Grid>
        </GroupBox>

        <TextBlock Grid.Row="3" x:Name="txtMessage" Margin="0,10,0,0" TextWrapping="Wrap" FontWeight="Bold"/>
    </Grid>
</Window>
